/** import { NgModule } from "@angular/core";
import { submodules } from '@modules/administrative/index'; //node_modules after install from package.json or import it locally
@NgModule({  imports: [submodules ]})
export class SubModule {} */